rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App uses Realtime Database; keep Firestore locked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
