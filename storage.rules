rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // App media uses Dropbox URLs; Storage is locked to authenticated reads only.
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
